<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FBallTeamQuiz" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@color/fball</item>
        <item name="android:navigationBarColor">@color/fball</item>
    </style>

    <style name="Theme.FBallTeamQuiz" parent="Base.Theme.FBallTeamQuiz" />
</resources>