<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.NiceFireworks" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@color/sky</item>
        <item name="android:navigationBarColor">@color/sky</item>
    </style>

    <style name="Theme.NiceFireworks" parent="Base.Theme.NiceFireworks" />
</resources>