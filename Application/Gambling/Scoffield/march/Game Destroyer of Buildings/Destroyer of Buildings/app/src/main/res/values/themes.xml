<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.DestroyerOfBuildings" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/buildings_area</item>
        <item name="android:navigationBarColor">@color/white</item>
    </style>

    <style name="Theme.DestroyerOfBuildings" parent="Base.Theme.DestroyerOfBuildings" />
</resources>